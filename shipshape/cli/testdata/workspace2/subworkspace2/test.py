class FooClass:
    def __init__():
        return "this should take self as the first argument"
        unreachable_assignment = 42

    def builtins():
        map(lambda x: x+1, [1, 2, 3])
        
    def iter():  
        return 3                # I think this is the best type of iterator 

return FooClass  
yield FooClass      







